# Optional: not used for judge/demo runs — judges enter their own key in the app UI.
# Create a key at https://aistudio.google.com/apikey (AQ. auth keys are supported).
# GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
APP_URL="MY_APP_URL"
